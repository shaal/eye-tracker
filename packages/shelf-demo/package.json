{
  "name": "@eye-tracker/shelf-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-only demo: gaze tracking over a simulated store shelf, no Electron and no native addon",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@eye-tracker/core": "*",
    "@mediapipe/tasks-vision": "^0.10.35"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  }
}
